# macOS
.DS_Store

# Original migration bundle (contents already unpacked into the repo)
files.zip

# Scratch
_unzipped/

# Local Netlify folder
.netlify

# Machine-specific Claude settings (keep launch.json shared)
.claude/settings.local.json
